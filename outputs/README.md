# Outputs

No real model outputs are included in this repository.

The scripts can write coefficient tables, sample-flow summaries, and audit files to this directory during a local analysis. Generated outputs are ignored by Git unless explicitly added.
